name: llm-training
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14.3
  - pip
  - pip:
    - torch

# Notes:
# - This environment.yml uses pip to install torch because prebuilt conda packages for Python 3.14 may be limited. 
# - If you need CUDA-enabled PyTorch, install the appropriate build after creating the environment (see https://pytorch.org/get-started/locally/).